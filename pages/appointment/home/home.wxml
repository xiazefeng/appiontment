<!--pages/appointment/home/home.wxml-->

<!-- <cu-custom bgColor="bg-gradual-pink" isBack="{{false}}">
  <view slot="content">预约</view>
</cu-custom> -->
<view class="cu-bar search bg-white ">
  <view class="search-form round">
    <text class="cuIcon-search"></text>
    <input type="text" placeholder="按搜索店名" confirm-type="search" value="{{searchTxt}}" bindinput='getSearchTxt' bindconfirm="searchSubmit"></input>
    <view class="action">
      <picker bindchange="PickerChange" bindcancel="PickerCancel" value="{{index}}" range="{{picker}}" range-key="tagName">
			<view class="picker">
				{{index?picker[index].tagName:'标签搜索'}}
			</view>
		</picker>
      <text class="cuIcon-triangledownfill my-margin" ></text>
    </view>
  </view>
  <!-- <view class="action">
      <button class="cu-btn bg-green shadow-blur round">搜索</button>
    </view> -->
</view>

<card 
isCard="no-card" 
info="{{cardList}}"
> 
</card>